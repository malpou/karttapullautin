[package]
name = "pullauta"
authors = ["Raphael Stefanini <raphael@routechoices.com>"]
version = "2.15.1"
edition = "2024"
description = "Tool to generates orienteering maps from classified LiDAR data"
readme = "README.md"
repository = "https://github.com/karttapullautin/karttapullautin"
license-file = "LICENSE"
keywords = ["map", "mapping", "lidar", "orienteering", "laz", "las"]
categories = ["command-line-utilities"]
publish = false

[features]
default = ["shapefile"]
shapefile = ["dep:tiny-skia", "dep:shapefile", "dep:zip"]

# Slower to compile, faster to run. Combine with RUSTFLAGS="-C target-cpu=native"
# for a build tuned to the local machine.
[profile.release]
lto = "fat"
codegen-units = 1

[dependencies]
image = { version = "0.25", default-features = false, features = [
	"rayon",
	"png",
	"jpeg",
] }

png = "0.18"

imageproc = { version = "0.27", default-features = false, features = [
	"rayon",
] }

las = { version = "0.11", features = ["laz", "laz-parallel"] }
rand = "0.10"
rust-ini = "0.21"
rustc-hash = "2.0"
shapefile = { version = "0.9.0", optional = true }
tiny-skia = { version = "0.12", optional = true }
zip = { version = "8.3", default-features = false, features = [
	"bzip2",
	"deflate",
	"deflate64",
	"lzma",
	"zstd",
	"xz",

], optional = true }
log = "0.4"
env_logger = "0.11"

serde = { version = "1", default-features = false, features = ["derive"] }
serde_json = "1"
anyhow = "1"
mimalloc = { version = "0.1", default-features = false }
bincode-next = { version = "2.1", default-features = false, features = ["std", "serde"] }

bytemuck = { version = "1.23", features = ["derive"] }
itertools = "0.15.0"

[build-dependencies]
typify = "0.8"
schemars = "0.8"
serde_json = "1"
syn = "3"
prettyplease = "0.3"
